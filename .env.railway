# Railway Environment Variables Template
# Copy this to Railway dashboard → Settings → Variables

# === REQUIRED ===

# Node Environment (production for Railway)
NODE_ENV=production

# Port (Railway auto-provides this, use Railway variable reference)
PORT=${{PORT}}

# Database URL (Use Railway PostgreSQL reference variable)
# After adding PostgreSQL service, use this format:
DATABASE_URL=${{PGDATABASE.DATABASE_URL}}
# OR if using external database:
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secrets (Generate using: openssl rand -base64 64)
ACCESS_TOKEN_SECRET=REPLACE_WITH_YOUR_64_CHAR_SECRET_HERE
REFRESH_TOKEN_SECRET=REPLACE_WITH_YOUR_DIFFERENT_64_CHAR_SECRET_HERE

# Session Secret (Generate using: openssl rand -base64 32)
SECRET_KEY=REPLACE_WITH_YOUR_32_CHAR_SECRET_HERE

# CORS - Your frontend URLs (comma-separated)
WEB_URL=https://your-frontend.vercel.app,https://your-domain.com

# === OPTIONAL (defaults provided) ===

# File Upload Settings
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Pagination
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=100

# === HOW TO GENERATE SECRETS ===
# Run in terminal:
# openssl rand -base64 64  # For JWT secrets
# openssl rand -base64 32  # For session secret
#
# Or use Node.js:
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
